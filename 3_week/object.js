const usuario = {
  username: "joaosoares",
  foto: "url",
  nome: "João",
};

const { username, nome } = usuario;
console.log(username); // equivalente a usuario.username
console.log(nome); // equivalente a usuario.name
