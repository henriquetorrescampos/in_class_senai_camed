// I can create an object without the necessity to create a class and instance as well

const usuario = {
  nome: "Bruno",
  idade: 24,
  email: "bruno@email.com",
};

const { nome } = usuario;

console.log(idade);
